services:
  postgres:
    image: 'postgres:17'
    restart: always
    volumes:
      - './postgres_data:/var/lib/postgresql/data'
    environment:
      POSTGRES_USER: Ana
      POSTGRES_DB: Plantas-Origen
      POSTGRES_PASSWORD: '123456'
    ports:
      - '5432:5432'